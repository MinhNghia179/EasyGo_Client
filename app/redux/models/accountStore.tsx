export interface IAccountStore {}

const initialState: IAccountStore = {};

const accountStore = {
  state: initialState,
  reducers: {},
  effects: dispatch => ({}),
};

export default accountStore;
