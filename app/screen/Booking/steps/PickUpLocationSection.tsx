import React from 'react';
import { View } from 'react-native';
import { Text } from '../../../components/Text';

interface IProps {}

const PickUpLocationSection = (props: IProps) => {
  return (
    <View>
      <Text>PickUpLocationSection</Text>
    </View>
  );
};

export default PickUpLocationSection;
