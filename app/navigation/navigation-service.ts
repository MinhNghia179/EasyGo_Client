const navigate = (routeName: string, params: any) => {
  return;
};

const getParam = (
  routeName: string,
  paramName: string,
  defaultValue: string,
) => {};

const _getNavigation = (route: string, params: any) => {};
